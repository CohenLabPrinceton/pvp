# library imports:
import time # not needed - for testing 
import automationhat

def update_controller(tracker):
    # Will take in tracked sensor readings to update controller.
    # Should also log prior controller commands. 
    
    # Some code for testing relays:
    '''
    if automationhat.is_automation_hat():
        timet = 0.01
        automationhat.relay.one.toggle()
        time.sleep(timet)
        automationhat.relay.one.toggle()
        time.sleep(timet)
    '''
    
    
    return